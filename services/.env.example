# CareConnect Services - Environment Variables (Example)
#
# Copy to .env in each service directory and customize

# Common
LOG_LEVEL=INFO
ENVIRONMENT=development

# Patient FHIR Service
USE_MOCK_FHIR=true
GCP_PROJECT_ID=careconnect-dev
GCP_REGION=northamerica-northeast1
FHIR_STORE_NAME=projects/{project}/locations/{region}/datasets/{dataset}/fhirStores/{store}

# Auth Identity Service
JWT_SECRET_NAME=projects/{project}/secrets/demo-jwt-secret/versions/latest

# QR Session Service
QR_TOKEN_TTL_SECONDS=300

# Doctor Finder Service
RATE_LIMIT_PER_MINUTE=10
