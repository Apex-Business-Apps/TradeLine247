name: db/migrate
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - supabase/migrations/**
  pull_request:
    branches: [main]
    paths:
      - supabase/migrations/**

jobs:
  migrate:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - uses: actions/checkout@v6
      - uses: supabase/setup-cli@v1
        with:
          version: "2.20.3"
      - name: Fail fast on main push when secrets missing
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && env.SUPABASE_ACCESS_TOKEN == ''
        run: echo "Missing SUPABASE_ACCESS_TOKEN on main push; fix repo secrets." && exit 1
      - name: Run migrations
        run: |
          if [ -n "$SUPABASE_ACCESS_TOKEN" ]; then
            echo "Remote migrate: enabled"
            echo "🔗 Linking Supabase project..."
            supabase link --project-ref "$SUPABASE_PROJECT_ID"
            echo "🚀 Applying migrations to remote database..."
            supabase db push --debug
          else
            echo "Remote migrate: skipped (no secrets); ran local validation"
            echo "🏠 Running local Supabase validation..."
            supabase init --yes 2>/dev/null || echo "Supabase already initialized"
            supabase db start
            supabase db reset --debug
            supabase db stop
          fi
