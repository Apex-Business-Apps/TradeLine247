# Instructions for Lovable AI

## ⛔ DO NOT "FIX" TYPESCRIPT ERRORS

### This Project's Intentional Configuration

```json
{
  "strict": false,
  "noImplicitAny": false,
  "noUnusedLocals": false,
  "noUnusedParameters": false
}
```

### Verification Commands

Before claiming there are TypeScript errors, **run these commands**:

```bash
npm run typecheck  # ✅ Should pass with 0 errors
npm run lint       # ✅ Should pass with 0 errors
npm run build      # ✅ Should succeed
```

### Current Status (as of 2024-12-05)

- ✅ `npm run typecheck` → **0 errors**
- ✅ `npm run lint` → **0 errors**
- ✅ `npm run build` → **Successful (15.77s)**

### Your LSP Settings Are Wrong

You are using **stricter TypeScript settings** than this project configures. Your complaints about:
- "Parameter X is unused" → Allowed by `noUnusedParameters: false`
- "Type 'any' is not allowed" → Allowed by `noImplicitAny: false`
- "Variable X is unused" → Allowed by `noUnusedLocals: false`

These are **FALSE POSITIVES** caused by your configuration mismatch.

### Required Behavior

1. **DO NOT** modify TypeScript unless explicitly instructed
2. **DO NOT** apply stricter rules than tsconfig.json specifies
3. **VERIFY** with `npm run typecheck` before claiming errors exist
4. **RESPECT** the project's intentional lax typing configuration
5. **STOP** trying to "fix" code that passes all actual validation

### Files with Protection Headers

The following files have explicit "DO NOT MODIFY" headers:
- `src/components/dashboard/NewDashboard.tsx`
- `src/pages/ClientDashboard.tsx`

**Respect these headers.** Do not override them.

### When to Actually Fix TypeScript

Only fix TypeScript if:
1. `npm run typecheck` shows actual errors (exit code ≠ 0)
2. `npm run build` fails due to type errors
3. User explicitly requests TypeScript fixes

### Summary

**There are no TypeScript errors.** Your LSP is misconfigured. Follow the project's tsconfig.json, not your own defaults.
